# SCP Golf Alpha runs entirely on local synthetic JSON data.
# No environment variables are required for the alpha.
# Reserved for future phases (hosted MCP, database, real course integrations).
# SCP_DATA_DIR=./src/data
